-- No-op: identical to `0003_spotify_requested_track.sql`. Drizzle generated this duplicate;
-- the table is already created by migration 0003. Keep this file so migration order stays valid.
SELECT 1;
